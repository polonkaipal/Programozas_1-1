#include <stdio.h>
#include <string.h>

void my_strfry(char s[])
{
    // ...
}

int main()
{
    char text[] = "C Programming";

    puts(text);
    my_strfry(text);
    puts(text);

    return 0;
}
