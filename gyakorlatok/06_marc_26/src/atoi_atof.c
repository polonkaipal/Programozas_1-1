#include "prog1.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(int argc, string argv[])
{
    string s = "20";
    int x = atoi(s);

    printf("%d\n", x);

    // ---------------

    string pi = "3.14159";
    double d = atof(pi);

    printf("%lf\n", d);

    return 0;
}
